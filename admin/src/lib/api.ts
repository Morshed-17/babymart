export { default as api } from "./config";

export {
  getAdminConfig,
  ADMIN_API_ENDPOINTS,
  adminApi,
  buildAdminQueryParams,
} from "./config";

// Export default of backward compatibility

export { default } from "./config";
